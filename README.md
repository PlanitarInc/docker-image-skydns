This repository contains skydns image, which intended to run on every host and provide
service discovery service.

# Instructions

Build:

```shell
make build
```

Push new image to registry:

```shell
make push
```

Clean:

```shell
make clean
```
